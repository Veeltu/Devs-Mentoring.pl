Higher Order Functions 
Functions operating using arguments from another functions, return etc
They don't modify existing functions, but return new values from them

Array.prototype.map
- create new array from array elements
- got 3 arguments element, index, Array

Array.prototype.filter
- use to filter date structure 

Array.prototype.reduce
- use to reduce of date structure contain
- have to got two arguments

Array.prototype.every
- check if / return true/false

Array.prototype.some 
- same like every, but check if only ONE hit 