// Zad. 3 
// Znajdź sposób, który umożliwia konwersję litery na odpowiednią liczbę ASCII oraz zamianę kodu ASCII na literę w JavaScript.

const string = "string"
const result = string.charCodeAt(0);
 
console.log(`The ASCII value is: ${result}`);
